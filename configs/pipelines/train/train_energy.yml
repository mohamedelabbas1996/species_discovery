exp_name: "'@{dataset.name}'_'@{network.name}'_'@{trainer.name}'_e'@{optimizer.num_epochs}'_lr'@{optimizer.lr}'_lam'@{trainer.lambda_energy}'_'@{mark}'/s'@{seed}'"

pipeline:
  name: train_energy

trainer:
  name: energy
  lambda_energy: 0.1
