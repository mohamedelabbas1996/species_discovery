exp_name: "'@{dataset.name}'_'@{network.name}'_'@{evaluator.name}'"
output_dir: "/network/scratch/y/yuyan.chen/species_discovery/features/'@{wandb.name}'"
save_output: True
merge_option: default # disabled if 'save_output' is False
mark: default # to mark the version of experiment
seed: 0

num_gpus: 1
num_workers: 0
num_machines: 1
machine_rank: 0

cluster:
  name: kmeans

network:
  pretrained: False

pipeline:
  name: test

evaluator:
  name: zero_shot_clustering

search_mode: 
  name: brent
  
recorder:
  save_scores: True
  save_csv: True

wandb:
  project: None
  entity: None
  name: None


k: 636

standardize: False
metric: pairwise

split_cost: 1
merge_cost: 2

pca: 
  n_components: 384





